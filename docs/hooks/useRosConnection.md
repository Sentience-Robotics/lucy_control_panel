# useRosConnection

**File:** `src/hooks/useRosConnection.hook.ts`

> TODO: Document the React hook that exposes ROS connection status and helpers to components.

## Purpose

## Returns

## Usage

```typescript
const { status, connect, disconnect } = useRosConnection();
```
